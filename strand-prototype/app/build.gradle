plugins { id 'com.android.application' }

android {
    namespace 'com.openai.strandterrain'
    compileSdk 35

    defaultConfig {
        applicationId 'com.openai.strandterrain'
        minSdk 26
        targetSdk 35
        versionCode 3
        versionName '0.2.1'
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}
